# .github/workflows/manual_crossplatform_build.yml
name: Manual Cross-platform Build Shotgun Code

on:
  workflow_dispatch:

env:
  NODE_OPTIONS: "--max-old-space-size=4096"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux
          - os: ubuntu-latest
            platform: linux/amd64
            safe: linux-amd64
            ext: ""
          - os: ubuntu-latest
            platform: linux/arm64
            safe: linux-arm64
            ext: ""
          # Windows
          - os: windows-latest
            platform: windows/amd64
            safe: windows-amd64
            ext: ".exe"
          - os: windows-latest
            platform: windows/arm64
            safe: windows-arm64
            ext: ".exe"
          # macOS
          - os: macos-latest
            platform: darwin/amd64
            safe: darwin-amd64
            ext: ""
          - os: macos-latest
            platform: darwin/arm64
            safe: darwin-arm64
            ext: ""
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          ref: main
          submodules: recursive

      - name: Build Wails app (${{ matrix.platform }})
        uses: dAppServer/wails-build-action@main
        id: wails_build
        with:
          build-name: ${{ matrix.name }}-${{ matrix.safe }}${{ matrix.ext }}
          build-platform: ${{ matrix.platform }}
          package: false
          go-version: '1.24'

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: shotgun_code-${{ matrix.safe }}${{ matrix.ext }}
          path: build/bin/