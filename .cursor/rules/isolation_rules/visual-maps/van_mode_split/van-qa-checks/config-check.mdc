---
description: configuration qa checks
globs: config-check.mdc
alwaysapply: false
---
# configuration qa checks

> **tldr:** this document outlines the quality assurance checks for project configuration files, ensuring that they are valid, complete, and follow best practices.

## ⚙️ configuration check workflow

```mermaid
graph td
    start["start<br>config checks"] --> locate["1️⃣ locate<br>config files"]
    locate --> validate["2️⃣ validate<br>syntax"]
    validate -->|valid| checkcomplete["3️⃣ check for<br>completeness"]
    validate -->|invalid| syntaxerror["🚨 syntax<br>error"]
    
    checkcomplete -->|complete| checkbest["4️⃣ check for<br>best practices"]
    checkcomplete -->|incomplete| incompleteerror["🚨 incomplete<br>config"]
    
    checkbest -->|pass| complete["✅ checks<br>complete"]
    checkbest -->|fail| bestpracticeerror["⚠️ best practice<br>violations"]
    
    syntaxerror --> reportse["report<br>syntax error"]
    incompleteerror --> reportie["report<br>incomplete config"]
    bestpracticeerror --> reportbpe["report<br>violations"]
    
    style start fill:#4da6ff,stroke:#0066cc,color:white
    style locate fill:#ffa64d,stroke:#cc7a30,color:white
    style validate fill:#4dbb5f,stroke:#36873f,color:white
    style checkcomplete fill:#d94dbb,stroke:#a3378a,color:white
    style checkbest fill:#4dbbbb,stroke:#368787,color:white
    style syntaxerror fill:#ff5555,stroke:#cc0000,color:white
    style incompleteerror fill:#ff5555,stroke:#cc0000,color:white
    style bestpracticeerror fill:#ffc107,stroke:#c79100,color:white
    style complete fill:#4caf50,stroke:#388e3c,color:white
```

## 📋 common configuration files

| file | purpose | validation tool |
|--------------|---------|-----------------|
| `package.json` | npm package configuration | `jsonlint` |
| `tsconfig.json` | typescript configuration | `tsc --noemit` |
| `webpack.config.js` | webpack build configuration | `webpack --validate` |
| `babel.config.js` | babel transpiler configuration | `babel --validate` |
| `.eslintrc.js` | eslint linter configuration | `eslint --validate-config` |
| `pom.xml` | maven project configuration | `mvn validate` |
| `build.gradle` | gradle build configuration | `gradle build` |

## ✅ verification complete

if all configuration files are valid, complete, and follow best practices, the configuration qa checks are complete.

```
configuration qa checks complete.
all configuration files are valid and well-formed.
```