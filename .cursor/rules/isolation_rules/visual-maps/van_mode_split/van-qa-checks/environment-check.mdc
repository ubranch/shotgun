---
description: environment qa checks
globs: environment-check.mdc
alwaysapply: false
---
# environment qa checks

> **tldr:** this document outlines the quality assurance checks for the development environment, ensuring that all required tools, versions, and configurations are in place.

## 🖥️ environment check workflow

```mermaid
graph td
    start["start<br>environment checks"] --> tools["1️⃣ check for<br>required tools"]
    tools -->|all present| versions["2️⃣ check tool<br>versions"]
    tools -->|missing| missingtools["🚨 missing<br>tools"]
    
    versions -->|all correct| config["3️⃣ check<br>configurations"]
    versions -->|incorrect| incorrectversions["⚠️ incorrect tool<br>versions"]
    
    config -->|all correct| complete["✅ checks<br>complete"]
    config -->|incorrect| incorrectconfig["🚨 incorrect<br>configurations"]
    
    missingtools --> reportmissing["report<br>missing tools"]
    incorrectversions --> reportversions["report<br>incorrect versions"]
    incorrectconfig --> reportconfig["report<br>incorrect configs"]
    
    style start fill:#4da6ff,stroke:#0066cc,color:white
    style tools fill:#ffa64d,stroke:#cc7a30,color:white
    style versions fill:#4dbb5f,stroke:#36873f,color:white
    style config fill:#d94dbb,stroke:#a3378a,color:white
    style missingtools fill:#ff5555,stroke:#cc0000,color:white
    style incorrectversions fill:#ffc107,stroke:#c79100,color:white
    style incorrectconfig fill:#ff5555,stroke:#cc0000,color:white
    style complete fill:#4caf50,stroke:#388e3c,color:white
```

## 📋 common environment checks

| tool | version command | configuration file |
|--------------|-----------------|--------------------|
| **node.js** | `node -v` | `.nvmrc` |
| **python** | `python --version` | `pyproject.toml` |
| **java** | `java -version` | `pom.xml` |
| **docker** | `docker --version` | `dockerfile` |
| **git** | `git --version` | `.gitconfig` |

## ✅ verification complete

if all required tools, versions, and configurations are correct, the environment qa checks are complete.

```
environment qa checks complete.
development environment is properly configured.
```